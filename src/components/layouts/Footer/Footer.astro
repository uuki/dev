---
import Link from '../../ui/Link/Link.astro';
import ListMenu from '../../objects/ListMenu/ListMenu.astro';
import styles from './Footer.module.scss';

// Social media icons (using Unicode for now, can be replaced with icon library later)
const socialItems = [
  { url: 'https://x.com/uuki_dev', icon: '𝕏', label: 'Twitter' },
  { url: 'https://github.com/uuki', icon: '', label: 'GitHub' },
  { url: '/atom.xml', icon: '', label: 'RSS' },
];

const currentYear = new Date().getFullYear();
---

<footer class={styles.footer}>
  <div class={styles.footer__content}>
    <ul class={styles.footer__socialList}>
      {socialItems.map((item) => (
        <li class={styles.footer__socialItem}>
          <Link href={item.url} variant="opacity" class={styles.footer__socialLink} aria-label={item.label}>
            {item.icon}
          </Link>
        </li>
      ))}
    </ul>
    <div class={styles.footer__menu}>
      <ListMenu />
    </div>
  </div>
  <div class={styles.footer__copyright}>
    <small>© {currentYear} uuki.dev</small>
  </div>
</footer>
