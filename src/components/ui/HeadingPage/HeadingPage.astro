---
import styles from './HeadingPage.module.scss';

interface Props {
  heading: 'h1' | 'h2';
  class?: string;
}

const {
  heading: TagName,
  class: className,
  ...props
} = Astro.props;

---
<TagName class:list={[styles.headingPage,`${className ?? ''}`]} {...props}>
  <slot />
</TagName>
