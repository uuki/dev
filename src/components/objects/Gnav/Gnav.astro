---
import { menu } from '@/data/constants';
import Link from '../../ui/Link/Link.astro';
import styles from './Gnav.module.scss';

const gnavData = menu.map((item) => ({
  ...item,
  isActive: item.path === Astro.url.pathname,
}));

---

<nav class={styles.gnav}>
  <ul class={styles.gnav__list}>
    {gnavData.map((item) => (
      <li class={styles.gnav__item}>
        <a href={item.path} class={item.isActive ? '-active' : ''}>{item.name}</a>
      </li>
    ))}
  </ul>
</nav>
