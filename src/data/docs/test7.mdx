---
title: 何某かの記事
slug: test7
description: test
author: uuki
date: '2022-02-20T12:57:50.996Z'
lastmod: '2022-02-27T07:09:05.618Z'
draft: true
tags:
  - OSS
image: /vercel.svg
origin: ''
url: ''
---

# h1 見出し aaa

## bbb

Lift($L$) can be determined by Lift Coefficient ($C_L$) like the following
equation.

$$
L = \frac{1}{2} \rho v^2 S C_L
$$

![Picture of the author](vercel.svg)

[Google](https://google.com)

|test|test|
|:-:|-:|
|aaa|bbb|

```js
export const getStaticProps: GetStaticProps<MDXProps, { slug: string }> = async ({ params }) => {
  if (!params?.slug) throw new Error('slug is missing')

  const filePath = path.join(process.cwd(), 'docs', `${params?.slug}.mdx`)
  const source = fs.readFileSync(filePath, 'utf-8')
  const { data: frontMatter, content } = matter(source)
  const mdxSource = await serialize(content)

  return JSON.parse(
    JSON.stringify({
      props: {
        meta: {
          ...frontMatter,
          slug: params.slug,
        },
        source: mdxSource,
      },
    })
  )
  // return {
  //   props: {
  //     meta: {
  //       ...frontMatter,
  //       slug: params.slug,
  //     },
  //     source: mdxSource,
  //   },
  // }
}

```