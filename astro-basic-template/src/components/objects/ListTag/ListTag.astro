---
import Tag from '../../ui/Tag/Tag.astro';
import styles from './ListTag.module.scss';

interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<ul class={styles.listTag}>
  {tags.map((tag) => (
    <li class={styles.listTag__item}>
      <Tag>{tag}</Tag>
    </li>
  ))}
</ul>
